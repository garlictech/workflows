{
    "dependencies": {
        "typedoc": "^0.14.0"
    },
    "scripts": {
        "typedoc": "make typedoc",
        "compile": "make compile",
        "build": "make build",
        "clean": "make clean",
        "start": "make start",
        "prettier": "make prettier",
        "prettier:check": "make prettier-check",
        "unittest": "make unittest",
        "unittest:ci": "make unittest-ci",
        "alltest": "make alltest",
        "alltest:ci": "make alltest-ci",
        "format:fix": "cd /app/project && pretty-quick --staged"
    }
}
